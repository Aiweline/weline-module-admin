# Backend Login Providers

This hook renders additional authentication providers above the backend login form.

Typical implementations:

- Google sign-in button
- Enterprise SSO button
- Custom identity provider entry

Implementation path example:

- `view/hooks/Weline_Admin/backend/partials/login/providers.phtml`
