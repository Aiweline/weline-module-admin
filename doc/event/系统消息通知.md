# Weline Admin 模块 - 事件文档

## 概述

本文档详细说明了 Weline Admin 模块提供的事件及其使用方法。这些事件允许其他模块与 Admin 模块进行交互，实现跨模块的功能扩展。

## 事件列表

### 1. Weline_Admin::msg - 系统消息通知事件

#### 基本信息

- **事件名称**：`Weline_Admin::msg`
- **事件类型**：系统消息通知
- **观察者类**：`Weline\Admin\Observer\SystemNotificationObserver`
- **数据模型**：`Weline\Admin\Model\System\SystemNotification`
- **配置文件**：`app/code/Weline/Admin/etc/event.xml`

#### 功能说明

用于跨模块发送系统消息通知，由 Weline_Admin 模块监听并写入消息中心。任何模块都可以触发此事件来发送系统消息给管理员。

#### 使用场景

- 订单状态变更通知
- 系统警告和错误提示
- 用户操作通知
- 系统维护通知
- 数据同步完成通知
- 审核流程通知
- 库存预警通知

#### 使用方法

##### 基本用法

```php
use Weline\Framework\Event\EventsManager;
use Weline\Framework\Manager\ObjectManager;

// 获取事件管理器实例
/** @var EventsManager $eventsManager */
$eventsManager = ObjectManager::getInstance(EventsManager::class);

// 发送系统消息
$eventsManager->dispatch('Weline_Admin::msg', [
    'data' => [
        'title'   => __('系统通知'),              // 必填：消息标题
        'content' => __('这是一条系统消息内容'),   // 必填：消息内容
        'is_read' => false,                       // 可选：是否已读，默认 false
        'is_icon' => 1,                          // 可选：使用图标头像，1=使用，0=不使用
        'is_img'  => 0,                          // 可选：使用图片头像，1=使用，0=不使用
        'avatar'  => 'ri-notification-line',     // 可选：头像内容（图标名或图片路径）
    ]
]);
```

#### 数据格式说明

| 字段 | 类型 | 必填 | 说明 | 默认值 |
|------|------|------|------|--------|
| title | string | 是 | 消息标题，最大长度120字符，建议使用 __() 进行国际化 | - |
| content | string | 是 | 消息内容，支持多行文本，建议使用 __() 进行国际化 | - |
| is_read | bool | 否 | 是否已读，false=未读，true=已读 | false |
| is_icon | int | 否 | 是否使用图标头像，1=使用，0=不使用 | 1 |
| is_img | int | 否 | 是否使用图片头像，1=使用，0=不使用 | 0 |
| avatar | string | 否 | 头像内容，图标名称（如 'ri-notification-line'）或图片路径 | 'ri-notification-line' |

#### 使用示例

##### 示例 1：使用图标头像

```php
$eventsManager->dispatch('Weline_Admin::msg', [
    'data' => [
        'title' => __('订单通知'),
        'content' => __('您有新的订单需要处理'),
        'is_icon' => 1,
        'is_img' => 0,
        'avatar' => 'ri-shopping-cart-line'  // RemixIcon 图标名称
    ]
]);
```

##### 示例 2：使用图片头像

```php
$eventsManager->dispatch('Weline_Admin::msg', [
    'data' => [
        'title' => __('用户消息'),
        'content' => __('用户提交了新的反馈'),
        'is_icon' => 0,
        'is_img' => 1,
        'avatar' => 'assets/images/users/avatar-1.jpg'  // 图片路径
    ]
]);
```

##### 示例 3：在控制器中使用

```php
<?php

namespace Your\Module\Controller;

use Weline\Framework\Event\EventsManager;
use Weline\Framework\Manager\ObjectManager;

class YourController
{
    /**
     * 发送系统消息示例
     */
    public function sendNotification(): void
    {
        /** @var EventsManager $eventsManager */
        $eventsManager = ObjectManager::getInstance(EventsManager::class);
        
        // 发送订单通知
        $eventsManager->dispatch('Weline_Admin::msg', [
            'data' => [
                'title' => __('新订单提醒'),
                'content' => __('订单编号：ORD-20241219-001 已创建，请及时处理。'),
                'is_read' => false,
                'is_icon' => 1,
                'avatar' => 'ri-shopping-bag-line'
            ]
        ]);
        
        // 发送系统警告
        $eventsManager->dispatch('Weline_Admin::msg', [
            'data' => [
                'title' => __('系统警告'),
                'content' => __('服务器磁盘使用率已达到 85%，请及时清理。'),
                'is_read' => false,
                'is_icon' => 1,
                'avatar' => 'ri-alert-line'
            ]
        ]);
    }
}
```

##### 示例 4：订单状态变更通知

```php
// 在订单状态变更时发送通知
public function updateOrderStatus($orderId, $newStatus): void
{
    // ... 更新订单状态的代码 ...
    
    /** @var EventsManager $eventsManager */
    $eventsManager = ObjectManager::getInstance(EventsManager::class);
    
    $eventsManager->dispatch('Weline_Admin::msg', [
        'data' => [
            'title' => __('订单状态变更'),
            'content' => __('订单 #%{orderId} 状态已变更为：%{status}', [
                'orderId' => $orderId,
                'status' => $newStatus
            ]),
            'is_read' => false,
            'is_icon' => 1,
            'avatar' => 'ri-shopping-bag-line'
        ]
    ]);
}
```

##### 示例 5：系统警告通知

```php
// 在系统检测到异常时发送警告
public function checkSystemHealth(): void
{
    $diskUsage = $this->getDiskUsage();
    
    if ($diskUsage > 85) {
        /** @var EventsManager $eventsManager */
        $eventsManager = ObjectManager::getInstance(EventsManager::class);
        
        $eventsManager->dispatch('Weline_Admin::msg', [
            'data' => [
                'title' => __('系统警告'),
                'content' => __('服务器磁盘使用率已达到 %{usage}%，请及时清理。', [
                    'usage' => $diskUsage
                ]),
                'is_read' => false,
                'is_icon' => 1,
                'avatar' => 'ri-alert-line'
            ]
        ]);
    }
}
```

#### 系统行为说明

##### 1. 数据验证

- 如果数据为空或不是数组，观察者会自动写入一条"错误"系统消息
- 如果缺少必填字段（title 或 content），也会写入错误消息
- 错误消息包含发送者信息，便于排查问题

##### 2. 去重机制

- 如果存在相同 `title` + `content` 的消息且为已读状态
- 系统会将该消息重置为"未读"状态，不会重复创建新记录
- 这样可以避免重复消息，同时确保重要消息不会被忽略

##### 3. 默认行为

- 如果不指定头像类型，默认使用图标头像
- 默认图标为 `ri-notification-line`
- 如果不指定 `is_read`，默认为 `false`（未读）

##### 4. 国际化支持

- 所有可见字符串（title、content）建议使用 `__()` 函数包装
- 这样可以支持多语言切换
- 框架会自动根据当前语言环境翻译消息内容

#### 注意事项

1. **必需字段**：`title` 和 `content` 是必需字段，缺少这些字段消息将不会被保存
2. **头像类型**：`is_icon` 和 `is_img` 不能同时为 1，如果同时设置，优先使用 `is_icon`
3. **默认行为**：如果不指定头像类型，默认使用图标头像（`is_icon=1`）
4. **错误处理**：消息保存失败不会影响主流程，错误会被静默处理（开发模式下会记录到错误日志）
5. **事件名称**：必须使用 `Weline_Admin::msg` 作为事件名称
6. **数据包装**：事件数据必须包装在 `data` 键中
7. **字符长度**：标题最大长度为 120 字符，超出部分会被截断
8. **性能考虑**：频繁发送消息可能影响性能，建议合理控制消息发送频率

#### 常用图标列表

以下是一些常用的 RemixIcon 图标名称，可用于消息头像：

- `ri-notification-line` - 通知（默认）
- `ri-shopping-cart-line` - 购物车/订单
- `ri-shopping-bag-line` - 购物袋
- `ri-alert-line` - 警告
- `ri-error-warning-line` - 错误警告
- `ri-information-line` - 信息
- `ri-checkbox-circle-line` - 成功/完成
- `ri-user-line` - 用户
- `ri-mail-line` - 邮件
- `ri-message-line` - 消息
- `ri-settings-line` - 设置
- `ri-database-line` - 数据库
- `ri-server-line` - 服务器

更多图标请参考 [RemixIcon 官网](https://remixicon.com/)

#### 相关文件

- **事件配置**：`app/code/Weline/Admin/etc/event.xml`
- **观察者类**：`app/code/Weline/Admin/Observer/SystemNotificationObserver.php`
- **数据模型**：`app/code/Weline/Admin/Model/System/SystemNotification.php`
- **事件文档配置**：`app/code/Weline/Admin/event.php`
- **模块文档**：`app/code/Weline/Admin/doc/README.md`
- **框架开发文档**：`docs/dev/开发文档.md`（第 13.4 节）

#### 故障排查

##### 问题：消息发送后没有显示

**可能原因：**
1. 数据格式不正确，缺少必填字段
2. 观察者未正确注册
3. 事件缓存未更新

**解决方法：**
1. 检查消息数据是否包含 `title` 和 `content` 字段
2. 检查 `etc/event.xml` 中是否正确配置了观察者
3. 运行 `php bin/w event:cache -f` 清除事件缓存
4. 检查系统消息表中是否有错误消息记录

##### 问题：消息重复显示

**可能原因：**
1. 多次触发相同的事件
2. 去重机制未生效

**解决方法：**
1. 检查代码中是否有重复的事件触发
2. 确保消息的 `title` 和 `content` 组合是唯一的
3. 如果确实需要发送相似消息，可以在标题或内容中添加时间戳或唯一标识

##### 问题：消息保存失败

**可能原因：**
1. 数据库连接问题
2. 数据表不存在或结构不正确
3. 字段值超出长度限制

**解决方法：**
1. 检查数据库连接是否正常
2. 运行 `php bin/w setup:upgrade` 更新数据库结构
3. 检查消息标题是否超过 120 字符
4. 查看错误日志获取详细错误信息

## 扩展开发

如果需要扩展系统消息功能，可以：

1. **自定义观察者**：创建新的观察者类来处理特定类型的消息
2. **消息分类**：在消息数据中添加 `type` 字段来区分不同类型的消息
3. **消息优先级**：添加 `priority` 字段来设置消息优先级
4. **消息过期**：添加 `expire_time` 字段来设置消息过期时间
5. **消息分组**：添加 `group` 字段来对消息进行分组显示

## 更新日志

- **2024-12-19**：初始版本，添加系统消息通知事件文档

## 相关资源

- [Weline Framework 事件系统文档](../../../Framework/doc/开发/服务器事件系统.md)
- [事件调试功能使用指南](../../../../docs/事件调试功能使用指南.md)
- [RemixIcon 图标库](https://remixicon.com/)

