<?php
/**@var \Weline\Framework\View\Template $this */
// 注意：此模板可能被嵌入到其他模板的 if 块中，因此不能使用 use 语句
// use 语句在 PHP 中必须在文件顶层，不能在控制结构内部

?>
<?php
$themeConfig = \Weline\Framework\Manager\ObjectManager::getInstance('\Weline\Backend\Block\ThemeConfig');
// 检查是否为水平布局，水平布局时不显示左侧菜单
$currentLayouts = $themeConfig->getOriginThemeConfig('layouts') ?? [];
$dataLayout = $currentLayouts['data-layout'] ?? '';
if ($dataLayout !== 'horizontal'):
    // 使用服务类获取菜单数据
    /**@var \Weline\Admin\Service\MenuRenderService $menuRenderService */
    $menuRenderService = \Weline\Framework\Manager\ObjectManager::getInstance(\Weline\Admin\Service\MenuRenderService::class);
    $adminPhrase = static fn(string $phrase): string => $menuRenderService->translateMenuTitle($phrase, 'Weline_Admin::left-sidebar');
    
    // 获取常用菜单数据
    $frequentMenusData = $menuRenderService->getFrequentMenus(20, 7);
    $recentMenus = $frequentMenusData['recentMenus'];
    $frequentMenus = $frequentMenusData['frequentMenus'];
    $hasFrequentMenus = $frequentMenusData['hasFrequentMenus'];
    
    // 获取菜单树
    $roleAccessMenus = $menuRenderService->getMenuTree();
    ?>
    <!-- ========== Left Sidebar Start ========== -->
    <div class="vertical-menu">

        <div data-simplebar class="h-100">

            <!--- Sidemenu -->
            <div id="sidebar-menu">
                <!-- 悬浮搜索和常用菜单容器 - 改造为菜单项形式 -->
                <div class="sticky-menu-section" id="sticky-menu-section">
                    <!-- Tab切换按钮（正常状态下显示） -->
                    <div class="menu-tabs-nav px-3 py-2">
                        <div class="d-flex gap-2 align-items-center" style="position: relative;">
                            <!-- 搜索Tab - 可展开为搜索栏（默认选中，放在前面） -->
                            <div class="search-tab-container" id="search-tab-container">
                                <!-- 搜索Tab按钮（初始状态） -->
                                <button type="button" 
                                        class="menu-tab-btn search-tab-btn active" 
                                        id="search-menu-tab"
                                        title="<?php echo $adminPhrase('搜索菜单'); ?>">
                                    <i class="mdi mdi-magnify" id="search-tab-icon"></i>
                                </button>
                                <!-- 搜索输入框（展开后显示） -->
                                <div class="search-tab-input-wrapper" id="search-tab-input-wrapper">
                                    <input type="text" 
                                           id="menu-search-input" 
                                           class="form-control form-control-sm search-tab-input" 
                                           placeholder="<?php echo $adminPhrase('搜索菜单...'); ?>"
                                           autocomplete="off">
                                    <i class="mdi mdi-magnify search-tab-close" id="search-tab-close-icon"></i>
                                </div>
                            </div>
                            <?php
                            // 显示常用菜单Tab按钮（如果存在常用菜单）
                            if ($hasFrequentMenus):
                            ?>
                            <!-- 常用菜单Tab（放在搜索后面） -->
                            <button type="button" 
                                    class="menu-tab-btn" 
                                    id="frequent-menu-tab"
                                    title="<?php echo $adminPhrase('常用菜单'); ?>">
                                <i class="mdi mdi-star-outline"></i>
                            </button>
                            <?php
                            endif;
                            ?>
                        </div>
                    </div>

                    <!-- Tab内容区域（正常状态下显示） -->
                    <div class="menu-tabs-content">
                         <div class="search-results-container" id="search-results-container" style="display: none;">
                            <!-- 搜索结果提示 -->
                            <div class="search-results-header px-3 py-2" id="search-results-header">
                                <h6 class="mb-0 text-muted fw-semibold">
                                    <i class="mdi mdi-magnify me-1"></i>
                                    <span id="search-results-text"><?php echo $adminPhrase('搜索结果'); ?></span>
                                </h6>
                            </div>
                            <!-- 搜索结果列表 -->
                            <div class="search-results-list" id="search-results-list"></div>
                        </div>
                        <!-- 常用菜单Tab内容（默认收起） -->


                        <!-- 常用菜单Tab内容（默认收起） -->
                        <div class="menu-tab-pane" id="frequent-tab-pane" style="display: none;">
                        <?php
                            if ($hasFrequentMenus):
                                echo $menuRenderService->renderFrequentTabContent($recentMenus, $frequentMenus);
                            endif;
                        ?>
                        </div>

                    </div>
                </div>
                <!-- 悬浮区域结束 -->

                <!-- Left Menu Start -->
                <ul class="metismenu list-unstyled" id="side-menu">
                    <!-- 搜索菜单项（以菜单形式实现，折叠状态下hover展开） -->
                    <li class="menu-search-item" id="menu-search-item" data-source="menu-search">
                        <a href="javascript: void(0);" class="has-arrow waves-effect" id="menu-search-link">
                            <i class="mdi mdi-magnify"></i>
                            <span><?php echo $adminPhrase('搜索菜单'); ?></span>
                        </a>
                        <ul class="sub-menu menu-search-submenu" aria-expanded="false" id="menu-search-submenu">
                            <li>
                                <div class="menu-search-content" id="menu-search-content">
                                    <!-- 搜索输入框 -->
                                    <div class="search-menu-input-wrapper px-3 py-2">
                                        <input type="text" 
                                               id="menu-search-input-menu" 
                                               class="form-control form-control-sm" 
                                               placeholder="<?php echo $adminPhrase('搜索菜单...'); ?>"
                                               autocomplete="off">
                                    </div>
                                    <!-- 搜索结果容器 -->
                                    <div class="search-results-container-menu" id="search-results-container-menu" style="display: none;">
                                        <div class="search-results-header px-3 py-2">
                                            <h6 class="mb-0 text-muted fw-semibold">
                                                <i class="mdi mdi-magnify me-1"></i>
                                                <span id="search-results-text-menu"><?php echo $adminPhrase('搜索结果'); ?></span>
                                            </h6>
                                        </div>
                                        <!-- 搜索结果列表 -->
                                        <div class="search-results-list-menu" id="search-results-list-menu"></div>
                                    </div>
                                </div>
                            </li>
                        </ul>
                    </li>
                    
                    <?php
                    // 常用菜单项（以菜单形式实现，折叠状态下hover展开）
                    if ($hasFrequentMenus):
                    ?>
                    <li class="menu-frequent-item" id="menu-frequent-item" data-source="menu-frequent">
                        <a href="javascript: void(0);" class="has-arrow waves-effect" id="menu-frequent-link">
                            <i class="mdi mdi-star-outline"></i>
                            <span><?php echo __('常用菜单'); ?></span>
                        </a>
                        <ul class="sub-menu menu-frequent-submenu" aria-expanded="false" id="menu-frequent-submenu">
                            <?php
                            // 使用服务渲染最近访问菜单
                            echo $menuRenderService->renderRecentMenus($recentMenus);
                            // 使用服务渲染访问最多菜单
                            echo $menuRenderService->renderFrequentMenus($frequentMenus);
                            ?>
                        </ul>
                    </li>
                    <?php
                    endif;
                    ?>
                    <?php
                    // 使用服务渲染主菜单
                    echo $menuRenderService->renderMenu($roleAccessMenus);
                    ?>
                </ul>
            </div>
            <!-- Sidebar -->
        </div>
    </div>

    <style>
        /* ==================== Left Sidebar Theme ==================== */
        .vertical-menu {
            background: var(--backend-color-sidebar-bg);
            border-right: 1px solid var(--backend-color-sidebar-border);
            box-shadow: var(--backend-sidebar-shadow);
            contain: layout style;
        }

        .vertical-menu [data-simplebar] {
            background: transparent;
        }

        #sidebar-menu {
            padding: 1rem 0.75rem 1.5rem;
        }

        #sidebar-menu > ul,
        #sidebar-menu .metismenu {
            padding-bottom: 1.25rem;
        }

        #sidebar-menu ul li {
            position: relative;
        }

        #sidebar-menu ul li.menu-title {
            padding: 0.8rem 0.95rem 0.35rem;
            color: var(--backend-color-text-tertiary);
            font-size: 0.72rem;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
        }

        #sidebar-menu ul li:not(.menu-title) > a {
            display: flex;
            align-items: center;
            gap: 0.8rem;
            min-height: 44px;
            margin-bottom: 0.2rem;
            padding: 0.72rem 0.95rem;
            color: var(--backend-color-sidebar-text);
            border-radius: var(--backend-border-radius-lg);
            transition: color 0.2s ease, background-color 0.2s ease, box-shadow 0.22s ease, transform 0.22s ease;
            will-change: transform, background-color;
        }

        #sidebar-menu ul li:not(.menu-title) > a i {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-width: 1.1rem;
            color: var(--backend-color-text-tertiary);
            transition: color 0.2s ease, transform 0.2s ease;
        }

        #sidebar-menu ul li:not(.menu-title) > a:hover {
            background: var(--backend-color-sidebar-hover);
            color: var(--backend-color-text-primary);
            transform: translateX(3px);
            box-shadow: inset 0 0 0 1px rgba(var(--backend-color-primary-rgb, 37, 99, 235), 0.05);
        }

        #sidebar-menu ul li:not(.menu-title) > a:hover i {
            color: var(--backend-color-primary);
            transform: scale(1.02);
        }

        #sidebar-menu ul li.mm-active > a,
        #sidebar-menu ul li.active > a {
            color: var(--backend-color-primary-dark);
            background: linear-gradient(135deg, var(--backend-color-primary-bg-light) 0%, var(--backend-color-primary-bg-subtle) 100%);
            box-shadow: inset 0 0 0 1px rgba(var(--backend-color-primary-rgb, 37, 99, 235), 0.08);
        }

        #sidebar-menu ul li > a.has-arrow {
            padding-right: 2.6rem;
        }

        #sidebar-menu ul li > a.has-arrow::after {
            content: "";
            position: absolute;
            top: 50%;
            right: 1rem;
            width: 0.6rem;
            height: 0.6rem;
            border-width: 0 1.5px 1.5px 0;
            border-style: solid;
            transform: translateY(-50%) rotate(45deg);
            transition: transform 0.22s ease, border-color 0.2s ease;
            pointer-events: none;
        }

        #sidebar-menu ul li.mm-active > a.has-arrow::after,
        #sidebar-menu ul li.active > a.has-arrow::after {
            transform: translateY(-50%) rotate(-135deg);
        }

        #sidebar-menu ul li.mm-active > a.has-arrow::after,
        #sidebar-menu ul li.active > a.has-arrow::after,
        #sidebar-menu ul li > a.has-arrow:hover::after {
            border-color: var(--backend-color-primary);
        }

        #sidebar-menu ul li.mm-active > a i,
        #sidebar-menu ul li.active > a i {
            color: var(--backend-color-primary);
        }

        #sidebar-menu ul li ul.sub-menu {
            margin-top: 0.15rem;
            padding-left: 0.45rem;
            display: none;
            visibility: hidden;
            overflow: hidden;
            max-height: 0;
            opacity: 0;
            transform: translateY(-6px);
            transform-origin: top;
            transition: opacity 0.22s ease, transform 0.24s ease;
            pointer-events: none;
        }

        #sidebar-menu ul li ul.sub-menu.mm-show,
        #sidebar-menu ul li.mm-active > ul.sub-menu,
        #sidebar-menu ul li.active > ul.sub-menu {
            display: block;
            visibility: visible;
            max-height: none;
            opacity: 1;
            transform: translateY(0);
            pointer-events: auto;
        }

        #sidebar-menu ul li ul.sub-menu li a {
            position: relative;
            min-height: 38px;
            padding: 0.58rem 0.85rem 0.58rem 2.35rem;
            color: var(--backend-color-text-secondary);
            border-radius: var(--backend-border-radius-md);
        }

        #sidebar-menu ul li ul.sub-menu li a::before {
            content: "";
            position: absolute;
            top: 50%;
            left: 1.2rem;
            width: 0.38rem;
            height: 0.38rem;
            border-radius: 50%;
            background: var(--backend-color-border-emphasis);
            transform: translateY(-50%);
        }

        #sidebar-menu ul li ul.sub-menu li a:hover::before,
        #sidebar-menu ul li ul.sub-menu li.mm-active > a::before,
        #sidebar-menu ul li ul.sub-menu li.active > a::before {
            background: var(--backend-color-primary);
        }

        .sticky-menu-section {
            margin: 0 0.25rem 0.85rem;
            border-radius: calc(var(--backend-border-radius-xl) + 2px);
            box-shadow: 0 12px 28px rgba(15, 23, 42, 0.06);
            overflow: hidden;
            backdrop-filter: blur(14px);
        }

        .sticky-menu-section.sticky-active {
            box-shadow: 0 18px 34px rgba(15, 23, 42, 0.1);
        }

        .menu-tab-btn {
            border: 1px solid transparent;
            border-radius: var(--backend-border-radius-md);
            background: var(--backend-color-bg-secondary);
            color: var(--backend-color-text-secondary);
            padding: 0.45rem 0.6rem;
            min-width: 32px;
            height: 32px;
            transition: all 0.2s ease;
        }

        .menu-tab-btn:hover,
        .menu-tab-btn.active {
            color: var(--backend-color-primary);
            background: var(--backend-color-primary-bg-subtle);
            border-color: rgba(var(--backend-color-primary-rgb, 37, 99, 235), 0.12);
            transform: translateY(-1px);
        }

        .search-tab-input,
        .search-tab-input-wrapper .form-control,
        .search-menu-input-wrapper .form-control {
            background: var(--backend-color-bg-secondary);
            border-color: var(--backend-color-input-border);
            color: var(--backend-color-text-primary);
            border-radius: var(--backend-border-radius-md);
            min-height: 32px;
            height: 32px;
            font-size: 0.82rem;
        }

        .search-tab-input::placeholder,
        .search-menu-input-wrapper .form-control::placeholder {
            color: var(--backend-color-text-placeholder);
        }

        .menu-tabs-content,
        .search-results-container,
        .search-results-container-menu {
            transition: opacity 0.2s ease, transform 0.22s ease;
        }

        .search-result-item a,
        .search-results-list-menu .search-result-item a,
        .search-results-list .search-result-item a {
            display: flex;
            align-items: center;
            gap: 0.7rem;
            padding: 0.56rem 0.8rem;
            color: var(--backend-color-text-secondary);
            border-radius: var(--backend-border-radius-md);
            transition: background-color 0.18s ease, color 0.18s ease, transform 0.18s ease;
        }

        .search-result-item a:hover,
        .search-results-list-menu .search-result-item a:hover,
        .search-results-list .search-result-item a:hover {
            color: var(--backend-color-text-primary);
            background: var(--backend-color-bg-secondary);
            transform: translateX(2px);
        }

        body.vertical-collpsed .vertical-menu {
            overflow: visible;
        }

        body.vertical-collpsed #sidebar-menu {
            padding-left: 0.5rem;
            padding-right: 0.5rem;
        }

        body.vertical-collpsed #sidebar-menu > ul > li > a {
            justify-content: center;
            padding-left: 0.8rem;
            padding-right: 0.8rem;
            border-radius: var(--backend-border-radius-lg);
        }

        body.vertical-collpsed #sidebar-menu > ul > li > a.has-arrow::after {
            display: none;
        }

        body.vertical-collpsed #sidebar-menu > ul > li > a:hover {
            transform: translateX(0) scale(1.02);
        }

        body.vertical-collpsed #sidebar-menu > ul > li > ul.sub-menu,
        body.vertical-collpsed .menu-search-submenu,
        body.vertical-collpsed .menu-frequent-submenu {
            display: block;
            top: 0;
            left: calc(100% + 0.75rem) !important;
            right: auto !important;
            width: min(280px, calc(100vw - 96px)) !important;
            min-width: 240px !important;
            max-width: 280px !important;
            padding: 0.55rem;
            border: 1px solid var(--backend-color-card-border);
            border-radius: var(--backend-border-radius-xl);
            background: var(--backend-color-card-bg) !important;
            box-shadow: var(--backend-dropdown-shadow);
            opacity: 0;
            visibility: hidden;
            transform: translate3d(-10px, 0, 0) scale(0.98);
            transform-origin: left center;
            pointer-events: none;
            transition: opacity 0.18s ease, transform 0.2s ease, visibility 0s linear 0.2s;
        }

        body.vertical-collpsed #sidebar-menu > ul > li:hover > ul.sub-menu,
        body.vertical-collpsed #sidebar-menu > ul > li.mm-active > ul.sub-menu,
        body.vertical-collpsed .menu-search-item:hover .menu-search-submenu,
        body.vertical-collpsed .menu-frequent-item:hover .menu-frequent-submenu,
        body.vertical-collpsed .menu-search-submenu:hover,
        body.vertical-collpsed .menu-frequent-submenu:hover {
            display: block;
            opacity: 1;
            visibility: visible;
            transform: translate3d(0, 0, 0) scale(1);
            pointer-events: auto;
            transition-delay: 0s;
        }

        body.vertical-collpsed #sidebar-menu > ul > li > ul.sub-menu > li > a,
        body.vertical-collpsed .menu-search-submenu li > a,
        body.vertical-collpsed .menu-frequent-submenu li > a {
            white-space: normal;
        }

        @media (prefers-reduced-motion: reduce) {
            #sidebar-menu ul li > a,
            #sidebar-menu ul li > a i,
            #sidebar-menu ul li ul.sub-menu,
            .menu-tab-btn,
            .menu-tabs-content,
            .search-results-container,
            .search-results-container-menu {
                transition: none !important;
            }
        }

        /* 悬浮搜索和常用菜单容器 */
        .sticky-menu-section {
            position: sticky;
            top: 0;
            z-index: 100;
            background-color: #ffffff;
            transition: all 0.3s ease;
            width: 100%; /* 默认宽度100%，不限制侧边栏 */
            box-sizing: border-box;
            /* 确保不会影响原生菜单的布局 */
            display: block;
        }
        
        /* 折叠状态下，完全隐藏sticky区域及其所有内容 */
        body.vertical-collpsed .sticky-menu-section {
            display: none !important;
            visibility: hidden !important;
            height: 0 !important;
            overflow: hidden !important;
            opacity: 0 !important;
            position: static !important;
            z-index: -1 !important;
            width: 0 !important;
        }
        
        body.vertical-collpsed .menu-tabs-content {
            display: none !important;
        }
        
        /* 正常状态下，隐藏菜单形式的搜索和常用菜单 */
        body:not(.vertical-collpsed) .menu-search-item,
        body:not(.vertical-collpsed) .menu-frequent-item {
            display: none !important;
        }
        
        /* 折叠状态下，显示菜单形式的搜索和常用菜单 */
        body.vertical-collpsed .menu-search-item,
        body.vertical-collpsed .menu-frequent-item {
            display: list-item !important;
            position: relative !important;
            white-space: nowrap !important;
        }
        
        /* 折叠状态下，菜单形式的搜索和常用菜单子菜单样式 - 宽度和位置与普通菜单hover展开一致 */
        body.vertical-collpsed .menu-search-submenu,
        body.vertical-collpsed .menu-frequent-submenu {
            width: 190px !important;
            min-width: 190px !important;
            max-width: 190px !important;
            position: absolute !important;
            right: 70px !important;
            background-color: #ffffff;
            box-shadow: -3px 5px 12px -4px rgba(18, 19, 21, 0.1);
            height: auto !important;
            z-index: 9999;
        }
        
        /* 折叠状态下，默认隐藏搜索子菜单，只在hover时显示（CSS控制） */
        body.vertical-collpsed .menu-search-item .menu-search-submenu {
            display: none !important;
            opacity: 0 !important;
            visibility: hidden !important;
        }
        
        /* hover时显示搜索子菜单 */
        body.vertical-collpsed .menu-search-item:hover .menu-search-submenu,
        body.vertical-collpsed .menu-search-item:hover .menu-search-submenu.mm-show {
            display: block !important;
            opacity: 1 !important;
            visibility: visible !important;
            height: auto !important;
            max-height: none !important;
            overflow: visible !important;
        }
        
        /* 当鼠标在子菜单上时也保持显示 */
        body.vertical-collpsed .menu-search-submenu:hover {
            display: block !important;
            opacity: 1 !important;
            visibility: visible !important;
        }
        
        /* 当输入框有焦点时，也保持显示（支持键盘操作） */
        body.vertical-collpsed .menu-search-item:has(#menu-search-input-menu:focus) .menu-search-submenu,
        body.vertical-collpsed .menu-search-submenu:has(#menu-search-input-menu:focus) {
            display: block !important;
            opacity: 1 !important;
            visibility: visible !important;
        }
        
        /* 确保mm-collapse和mm-show类不影响高度 */
        body.vertical-collpsed .menu-search-submenu.mm-collapse.mm-show {
            display: block !important;
            height: auto !important;
            max-height: none !important;
            overflow: visible !important;
            min-height: auto !important;
        }
        
        /* 移除mm-collapsing状态的限制 */
        body.vertical-collpsed .menu-search-submenu.mm-collapsing {
            height: auto !important;
            overflow: visible !important;
        }
        
        /* 搜索内容区域阻止点击冒泡 */
        .menu-search-content {
            pointer-events: auto;
        }
        
        .menu-search-content * {
            pointer-events: auto;
        }
        
        /* 常用菜单子菜单项样式 */
        .menu-frequent-submenu .frequent-menu-group-header-item {
            padding: 0;
        }
        
        .menu-frequent-submenu .frequent-menu-group-header {
            background-color: #f8f9fa;
            border-bottom: 1px solid #e9ecef;
            margin: 0;
        }
        
        .menu-frequent-submenu .frequent-menu-group-header h6 {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: #6c757d;
            margin: 0;
        }
        
        .menu-frequent-submenu .frequent-menu-item a {
            padding: 7px 14px;
            display: flex;
            align-items: center;
            color: #74788d;
            text-decoration: none;
            transition: all 0.2s;
            font-size: 0.84rem;
        }
        
        .menu-frequent-submenu .frequent-menu-item a:hover {
            color: #0bb197;
            background-color: #f8f9fa;
        }
        
        .menu-frequent-submenu .frequent-menu-item a i {
            margin-right: 7px;
            font-size: 1rem;
            width: 18px;
            text-align: center;
        }
        
        .menu-frequent-submenu .frequent-menu-count {
            font-size: 0.75rem;
            color: #6c757d;
            background-color: #e9ecef;
            padding: 2px 6px;
            border-radius: 10px;
            font-weight: 600;
            margin-left: auto;
        }
        
        body[data-sidebar="dark"] .menu-frequent-submenu {
            background-color: #1b2c3f;
        }
        
        body[data-sidebar="dark"] .menu-frequent-submenu .frequent-menu-group-header {
            background-color: rgba(255, 255, 255, 0.03);
            border-bottom-color: rgba(255, 255, 255, 0.1);
        }
        
        body[data-sidebar="dark"] .menu-frequent-submenu .frequent-menu-group-header h6 {
            color: #8590a5;
        }
        
        body[data-sidebar="dark"] .menu-frequent-submenu .frequent-menu-item a {
            color: #8590a5;
        }
        
        body[data-sidebar="dark"] .menu-frequent-submenu .frequent-menu-item a:hover {
            color: #d7e4ec;
            background-color: rgba(255, 255, 255, 0.1);
        }
        
        body[data-sidebar="dark"] .menu-frequent-submenu .frequent-menu-count {
            color: #8590a5;
            background-color: rgba(255, 255, 255, 0.1);
        }
        
        body.vertical-collpsed[data-sidebar="dark"] .menu-search-submenu,
        body.vertical-collpsed[data-sidebar="dark"] .menu-frequent-submenu,
        body[data-sidebar="dark"].vertical-collpsed .menu-search-submenu,
        body[data-sidebar="dark"].vertical-collpsed .menu-frequent-submenu {
            background-color: #1b2c3f;
        }
        
        /* 折叠状态下，菜单搜索和常用菜单内容样式 */
        body.vertical-collpsed .menu-search-content,
        body.vertical-collpsed .menu-frequent-content {
            padding: 0;
        }
        
        body.vertical-collpsed .search-menu-input-wrapper input {
            width: 100%;
            border: 1px solid #e9ecef;
            border-radius: 10px;
            padding: 6px 10px;
            font-size: 13px;
        }
        
        body.vertical-collpsed[data-sidebar="dark"] .search-menu-input-wrapper input,
        body[data-sidebar="dark"].vertical-collpsed .search-menu-input-wrapper input {
            background-color: #1b2c3f;
            border-color: rgba(255, 255, 255, 0.1);
            color: #fff;
        }
        
        /* 正常状态下菜单搜索输入框的暗色主题适配 */
        body[data-sidebar="dark"] #menu-search-input-menu,
        body[data-sidebar="dark"] .search-menu-input-wrapper .form-control {
            background-color: #1b2c3f !important;
            border-color: rgba(255, 255, 255, 0.1) !important;
            color: #fff !important;
        }
        
        body[data-sidebar="dark"] #menu-search-input-menu::placeholder,
        body[data-sidebar="dark"] .search-menu-input-wrapper .form-control::placeholder {
            color: rgba(255, 255, 255, 0.5) !important;
        }
        
        body[data-sidebar="dark"] #menu-search-input-menu:focus,
        body[data-sidebar="dark"] .search-menu-input-wrapper .form-control:focus {
            background-color: #1b2c3f !important;
            border-color: rgba(255, 255, 255, 0.2) !important;
            color: #fff !important;
            box-shadow: 0 0 0 0.2rem rgba(215, 228, 236, 0.25) !important;
        }
        
        /* 确保menu-search-content始终可见，即使有搜索结果 */
        body.vertical-collpsed .menu-search-content {
            display: block !important;
            visibility: visible !important;
            opacity: 1 !important;
            height: auto !important;
        }
        
        /* 确保搜索输入框区域始终可见 */
        body.vertical-collpsed .menu-search-content .search-menu-input-wrapper {
            display: block !important;
            visibility: visible !important;
            opacity: 1 !important;
        }
        
        /* 搜索结果列表样式 */
        .search-results-list,
        .search-results-list-menu {
            max-height: 300px;
            overflow-y: auto;
        }
        
        .search-result-item {
            border-bottom: 1px solid #e9ecef;
        }
        
        .search-result-item:last-child {
            border-bottom: none;
        }
        
        .search-result-item a {
            color: #74788d;
            text-decoration: none;
            transition: all 0.2s;
        }
        
        .search-result-item a:hover {
            color: #0bb197;
            background-color: #f8f9fa;
        }
        
        body[data-sidebar="dark"] .search-result-item {
            border-bottom-color: rgba(255, 255, 255, 0.1);
        }
        
        body[data-sidebar="dark"] .search-result-item a {
            color: #8590a5;
        }
        
        body[data-sidebar="dark"] .search-result-item a:hover {
            color: #d7e4ec;
            background-color: rgba(255, 255, 255, 0.1);
        }
        
        /* 折叠状态下，Tab导航只显示图标，紧凑布局 */
        body.vertical-collpsed .menu-tabs-nav {
            padding: 8px 4px;
            display: flex;
            justify-content: center;
            flex-direction: column;
            gap: 4px;
        }
        
        body.vertical-collpsed .menu-tabs-nav .d-flex {
            flex-direction: column;
            gap: 4px;
        }
        
        /* 折叠状态下，Tab按钮只显示图标，居中 */
        body.vertical-collpsed .menu-tab-btn {
            width: 100%;
            min-width: 36px;
            height: 36px;
            padding: 8px;
            justify-content: center;
        }
        
        /* 折叠状态下，搜索容器只显示图标按钮 */
        body.vertical-collpsed .search-tab-container {
            width: 100%;
            justify-content: center;
            pointer-events: auto !important; /* 确保可以hover */
            position: relative;
            z-index: 1;
        }
        
        /* 确保搜索tab按钮可以hover */
        body.vertical-collpsed #search-menu-tab {
            pointer-events: auto !important;
            position: relative;
            z-index: 2;
        }
        
        /* 确保常用菜单tab按钮可以hover */
        body.vertical-collpsed #frequent-menu-tab {
            pointer-events: auto !important;
            position: relative;
            z-index: 2;
        }
        
        /* 确保所有tab按钮在折叠状态下可交互 */
        body.vertical-collpsed .menu-tab-btn {
            pointer-events: auto !important;
        }
        
        body.vertical-collpsed .search-tab-input-wrapper {
            display: none !important;
            pointer-events: none !important;
        }
        
        /* 折叠状态下，内容区域向右展开的容器 */
        body.vertical-collpsed .menu-tabs-content.expanded-right {
            display: block !important;
            position: fixed;
            left: 70px; /* 侧边栏折叠后的宽度 */
            top: auto;
            background-color: #ffffff;
            box-shadow: 2px 2px 12px rgba(0, 0, 0, 0.2);
            min-width: 280px;
            max-width: 320px;
            z-index: 1050;
            border-radius: 4px;
            margin-top: 0;
            padding: 0;
            overflow: visible;
            pointer-events: auto; /* 确保可以交互 */
            opacity: 1;
            visibility: visible;
            transition: opacity 0.2s ease, visibility 0.2s ease;
        }
        
        /* 确保展开区域内的所有元素都可以交互 */
        body.vertical-collpsed .menu-tabs-content.expanded-right * {
            pointer-events: auto;
        }
        
        body.vertical-collpsed[data-sidebar="dark"] .menu-tabs-content.expanded-right,
        body[data-sidebar="dark"].vertical-collpsed .menu-tabs-content.expanded-right {
            background-color: #1b2c3f;
        }
        
        
        /* 悬浮状态样式 */
        .sticky-menu-section.sticky-active {
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }
        
        body[data-sidebar="dark"] .sticky-menu-section {
            background-color: #1b2c3f;
        }
        
        body[data-sidebar="dark"] .sticky-menu-section.sticky-active {
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
        }
        
        /* Tab导航样式 */
        .menu-tabs-nav {
            border-bottom: 1px solid #e9ecef;
            background-color: transparent;
            padding: 0.7rem 0.85rem 0.55rem;
        }
        
        body[data-sidebar="dark"] .menu-tabs-nav {
            border-bottom-color: rgba(255, 255, 255, 0.1);
            background-color: rgba(255, 255, 255, 0.03);
        }
        
        .menu-tab-btn {
            border: none;
            background: transparent;
            color: #6c757d;
            font-size: 16px;
            padding: 6px 10px;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.2s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            min-width: 32px;
            height: 32px;
        }
        
        .menu-tab-btn:hover {
            background-color: #e9ecef;
            color: #0bb197;
        }
        
        .menu-tab-btn.active {
            background-color: transparent;
            color: #0bb197;
        }
        
        .menu-tab-btn.active i {
            color: #0bb197;
        }
        
        body[data-sidebar="dark"] .menu-tab-btn {
            color: #8590a5;
            background-color: transparent;
        }
        
        body[data-sidebar="dark"] .menu-tab-btn:hover {
            background-color: transparent;
            color: #d7e4ec;
        }
        
        body[data-sidebar="dark"] .menu-tab-btn.active {
            background-color: transparent;
            color: #0bb197;
        }
        
        body[data-sidebar="dark"] .menu-tab-btn.active i {
            color: #0bb197;
        }
        
        /* 搜索Tab容器 - 可展开 */
        .search-tab-container {
            position: relative;
            display: flex;
            align-items: center;
            flex: 1;
            min-width: 0;
            min-height: 32px;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        /* 搜索Tab按钮 */
        .search-tab-btn {
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            flex-shrink: 0;
        }
        
        /* 搜索输入框包装器 - 默认隐藏 */
        .search-tab-input-wrapper {
            position: absolute;
            left: 0;
            top: 0;
            right: 0;
            width: 0;
            max-width: 100%;
            opacity: 0;
            visibility: hidden;
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            display: flex;
            align-items: center;
            height: 32px;
            background-color: #ffffff;
            border-radius: 12px;
            border: 1px solid transparent;
        }
        
        body[data-sidebar="dark"] .search-tab-input-wrapper {
            background-color: #1b2c3f;
        }
        
        /* 展开状态：输入框显示，Tab按钮隐藏 */
        .search-tab-container.expanded .search-tab-btn {
            opacity: 0;
            visibility: hidden;
            width: 0;
            padding: 0;
            min-width: 0;
            margin: 0;
        }
        
        .search-tab-container.expanded .search-tab-input-wrapper {
            width: 100%;
            opacity: 1;
            visibility: visible;
            border-color: #e9ecef;
        }
        
        body[data-sidebar="dark"] .search-tab-container.expanded .search-tab-input-wrapper {
            border-color: rgba(255, 255, 255, 0.1);
        }
        
        /* 搜索输入框 */
        .search-tab-input {
            border: none;
            background: transparent;
            color: #495057;
            font-size: 13px;
            padding: 0 30px 0 10px;
            height: 100%;
            width: 100%;
            outline: none;
        }
        
        body[data-sidebar="dark"] .search-tab-input {
            color: #fff;
            background-color: transparent !important;
        }
        
        /* 确保 form-control 类不会覆盖暗色主题样式 */
        body[data-sidebar="dark"] #menu-search-input,
        body[data-sidebar="dark"] .search-tab-input-wrapper .form-control {
            background-color: transparent !important;
            color: #fff !important;
        }
        
        .search-tab-input::placeholder {
            color: #adb5bd;
        }
        
        body[data-sidebar="dark"] .search-tab-input::placeholder,
        body[data-sidebar="dark"] #menu-search-input::placeholder {
            color: rgba(255, 255, 255, 0.5) !important;
        }
        
        .search-tab-input:focus {
            border-color: #0bb197;
            box-shadow: 0 0 0 0.2rem rgba(11, 177, 151, 0.25);
        }
        
        body[data-sidebar="dark"] .search-tab-input:focus,
        body[data-sidebar="dark"] #menu-search-input:focus {
            border-color: #d7e4ec;
            box-shadow: 0 0 0 0.2rem rgba(215, 228, 236, 0.25);
            background-color: transparent !important;
            color: #fff !important;
        }
        
        /* 搜索Tab关闭图标 */
        .search-tab-close {
            position: absolute;
            right: 9px;
            top: 50%;
            transform: translateY(-50%);
            color: #6c757d;
            font-size: 15px;
            cursor: pointer;
            opacity: 0.75;
            transition: all 0.2s ease;
        }
        
        .search-tab-close:hover {
            color: #0bb197;
            opacity: 1;
        }
        
        body[data-sidebar="dark"] .search-tab-close {
            color: #8590a5;
        }
        
        body[data-sidebar="dark"] .search-tab-close:hover {
            color: #d7e4ec;
        }
        
        /* 搜索结果容器 */
        .search-results-container {
            border-top: 1px solid #e9ecef;
        }
        
        body[data-sidebar="dark"] .search-results-container {
            border-top-color: rgba(255, 255, 255, 0.1);
        }
        
        /* Tab内容区域 */
        .menu-tabs-content {
            position: relative;
            transition: max-height 0.3s ease, opacity 0.3s ease, padding 0.3s ease;
            overflow: hidden;
            max-height: 1000px;
            opacity: 1;
        }
        
        /* 收起内容区域（在所有状态下都生效） */
        .menu-tabs-content.collapsed {
            max-height: 0 !important;
            opacity: 0 !important;
            padding: 0 !important;
            overflow: hidden !important;
        }
        
        /* 悬浮后收起内容区域（兼容旧逻辑） */
        .sticky-menu-section.sticky-active .menu-tabs-content.collapsed {
            max-height: 0;
            opacity: 0;
            overflow: hidden;
        }
        
        .menu-tab-pane {
            animation: fadeIn 0.3s ease;
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
        
        
        /* 常用菜单区域样式 - 浅色主题（默认） */
        .frequent-menus-section {
            border-bottom: 1px solid #e9ecef;
        }
        
        /* 常用菜单分组 */
        .frequent-menu-group {
            border-bottom: 1px solid #e9ecef;
        }
        
        .frequent-menu-group:last-child {
            border-bottom: none;
        }
        
        /* 分组头部 */
        .frequent-menu-group-header {
            background-color: #f8f9fa;
            border-bottom: 1px solid #e9ecef;
            position: sticky;
            top: 0;
            z-index: 1;
        }
        
        .frequent-menu-group-header h6 {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: #6c757d;
            margin: 0;
        }
        
        body[data-sidebar="dark"] .frequent-menu-group {
            border-bottom-color: rgba(255, 255, 255, 0.1);
        }
        
        body[data-sidebar="dark"] .frequent-menu-group-header {
            background-color: rgba(255, 255, 255, 0.03);
            border-bottom-color: rgba(255, 255, 255, 0.1);
        }
        
        body[data-sidebar="dark"] .frequent-menu-group-header h6 {
            color: #8590a5;
        }
        
        .frequent-menus-list {
            scrollbar-width: thin;
            scrollbar-color: #adb5bd transparent;
        }
        
        .frequent-menus-list::-webkit-scrollbar {
            width: 4px;
        }
        
        .frequent-menus-list::-webkit-scrollbar-track {
            background: transparent;
        }
        
        .frequent-menus-list::-webkit-scrollbar-thumb {
            background-color: #adb5bd;
            border-radius: 2px;
        }
        
        .frequent-menu-item a {
            color: #74788d;
            text-decoration: none;
            transition: all 0.2s;
            border-left: 3px solid transparent;
        }
        
        .frequent-menu-item a:hover {
            color: #383c40;
            background-color: #f8f9fa;
            border-left-color: #0bb197;
        }
        
        .frequent-menu-item a i {
            font-size: 1.1rem;
            width: 20px;
            text-align: center;
            color: #505d69;
            opacity: 0.75;
        }
        
        /* 访问次数计数样式 */
        .frequent-menu-count {
            font-size: 0.75rem;
            color: #6c757d;
            background-color: #e9ecef;
            padding: 2px 6px;
            border-radius: 10px;
            font-weight: 600;
            min-width: 24px;
            text-align: center;
            flex-shrink: 0;
        }
        
        .frequent-menu-item a:hover .frequent-menu-count {
            background-color: #dee2e6;
            color: #495057;
        }
        
        /* 深色主题下的常用菜单区域 */
        body[data-sidebar="dark"] .frequent-menus-section {
            border-bottom-color: rgba(255, 255, 255, 0.1);
        }
        
        body[data-sidebar="dark"] .frequent-menus-header {
            background-color: rgba(255, 255, 255, 0.03);
            border-bottom-color: rgba(255, 255, 255, 0.05);
        }
        
        body[data-sidebar="dark"] .frequent-menus-header h6 {
            color: #8590a5;
        }
        
        body[data-sidebar="dark"] .frequent-menus-list {
            scrollbar-color: rgba(255, 255, 255, 0.2) transparent;
        }
        
        body[data-sidebar="dark"] .frequent-menus-list::-webkit-scrollbar-thumb {
            background-color: rgba(255, 255, 255, 0.2);
        }
        
        body[data-sidebar="dark"] .frequent-menu-item a {
            color: #8590a5;
        }
        
        body[data-sidebar="dark"] .frequent-menu-item a:hover {
            color: #d7e4ec;
            background-color: rgba(255, 255, 255, 0.1);
        }
        
        body[data-sidebar="dark"] .frequent-menu-item a i {
            color: #8590a5;
        }
        
        body[data-sidebar="dark"] .frequent-menu-count {
            color: #8590a5;
            background-color: rgba(255, 255, 255, 0.1);
        }
        
        body[data-sidebar="dark"] .frequent-menu-item a:hover .frequent-menu-count {
            background-color: rgba(255, 255, 255, 0.2);
            color: #d7e4ec;
        }
        
        /* 搜索结果头部样式 */
        .search-results-header {
            border-bottom: 1px solid #e9ecef;
            background-color: #f8f9fa;
        }
        
        body[data-sidebar="dark"] .search-results-header {
            border-bottom-color: rgba(255, 255, 255, 0.1);
            background-color: rgba(255, 255, 255, 0.03);
        }
        
        .search-results-header h6 {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: #6c757d;
        }
        
        body[data-sidebar="dark"] .search-results-header h6 {
            color: #8590a5;
        }
        
        /* 搜索高亮样式 */
        .menu-item-matched {
            background-color: rgba(11, 177, 151, 0.15) !important;
        }
        
        body[data-sidebar="dark"] .menu-item-matched {
            background-color: rgba(215, 228, 236, 0.15) !important;
        }
        
        .menu-item-hidden {
            display: none !important;
        }
        
        /* 主菜单项hover样式 - 与常用菜单保持一致 */
        #sidebar-menu ul li:not(.menu-title) > a {
            border-left: 3px solid transparent;
            transition: all 0.2s;
        }
        
        #sidebar-menu ul li:not(.menu-title) > a:hover {
            color: #0bb197 !important;
            background-color: #f8f9fa;
            border-left-color: #0bb197;
        }
        
        #sidebar-menu ul li:not(.menu-title) > a:hover i {
            color: #0bb197 !important;
        }
        
        /* 深色主题下的主菜单hover样式 */
        body[data-sidebar="dark"] #sidebar-menu ul li:not(.menu-title) > a:hover {
            color: #d7e4ec !important;
            background-color: rgba(255, 255, 255, 0.1);
            border-left-color: #d7e4ec;
        }
        
        body[data-sidebar="dark"] #sidebar-menu ul li:not(.menu-title) > a:hover i {
            color: #d7e4ec !important;
        }
        
        /* 搜索结果键盘选中样式 */
        .menu-item-keyboard-selected {
            background-color: rgba(11, 177, 151, 0.2) !important;
            outline: 2px solid #0bb197;
            outline-offset: -2px;
        }
        
        body[data-sidebar="dark"] .menu-item-keyboard-selected {
            background-color: rgba(215, 228, 236, 0.2) !important;
            outline-color: #d7e4ec;
        }
        
        /* 搜索无结果提示 */
        .search-no-results {
            padding: 2rem 1rem;
            text-align: center;
            color: #6c757d;
        }
        
        body[data-sidebar="dark"] .search-no-results {
            color: #8590a5;
        }
        
        /* 暗色模式下侧边栏菜单整体背景色 - 与搜索栏一致 */
        body[data-sidebar="dark"] .vertical-menu {
            background-color: #1b2c3f !important;
        }
        
        body[data-sidebar="dark"] #sidebar-menu {
            background-color: #1b2c3f !important;
        }
        
        body[data-sidebar="dark"] .vertical-menu [data-simplebar] {
            background-color: #1b2c3f !important;
        }
        
        body[data-sidebar="dark"] .navbar-brand-box {
            background-color: #1b2c3f !important;
        }
        
        body[data-sidebar="dark"] .navbar-brand-box.text-center {
            background-color: #1b2c3f !important;
        }
        
        /* 修复hover菜单显示 - 使用与原CSS相同的选择器格式 */
        body.vertical-collpsed .vertical-menu #sidebar-menu > ul > li:hover > ul {
            display: block !important;
            position: absolute !important;
            right: 70px !important;
            width: 190px !important;
            z-index: 9999 !important;
        }
        
        /* 三级菜单hover显示 */
        body.vertical-collpsed .vertical-menu #sidebar-menu > ul > li:hover > ul ul {
            display: block !important;
            position: absolute !important;
            left: 190px !important;
            z-index: 10000 !important;
        }

        /* ===== Theme Variable Compatibility Overrides ===== */
        .sticky-menu-section {
            background: var(--backend-color-card-bg, #ffffff) !important;
            border: 1px solid var(--backend-color-border-default, #d8dee8) !important;
            box-shadow: var(--backend-shadow-md, 0 8px 20px rgba(15, 23, 42, 0.1)) !important;
        }

        .menu-tabs-nav {
            border-bottom: 1px solid var(--backend-color-border-light, #e7ecf3) !important;
            background: color-mix(in srgb, var(--backend-color-card-bg, #ffffff) 92%, var(--backend-color-bg-secondary, #f4f7fb) 8%) !important;
        }

        .menu-tab-btn {
            color: var(--backend-color-text-secondary, #6b7280) !important;
            background: var(--backend-color-bg-secondary, #f4f7fb) !important;
            border-color: var(--backend-color-border-default, #d8dee8) !important;
        }

        .menu-tab-btn:hover,
        .menu-tab-btn.active {
            color: var(--backend-color-primary, #0bb197) !important;
            background: var(--backend-color-primary-bg-subtle, rgba(11, 177, 151, 0.12)) !important;
            border-color: color-mix(in srgb, var(--backend-color-primary, #0bb197) 30%, var(--backend-color-border-default, #d8dee8) 70%) !important;
        }

        .search-tab-input-wrapper {
            background: var(--backend-color-card-bg, #ffffff) !important;
            border-color: var(--backend-color-input-border, #d8dee8) !important;
        }

        .search-tab-input,
        .search-menu-input-wrapper .form-control {
            color: var(--backend-color-text-primary, #111827) !important;
        }

        .search-results-container,
        .search-results-header,
        .search-result-item {
            border-color: var(--backend-color-border-light, #e7ecf3) !important;
        }

        .search-results-header {
            background: var(--backend-color-bg-secondary, #f4f7fb) !important;
        }

        .search-results-header h6,
        .search-no-results {
            color: var(--backend-color-text-secondary, #6b7280) !important;
        }

        .search-result-item a {
            color: var(--backend-color-text-secondary, #6b7280) !important;
        }

        .search-result-item a:hover {
            color: var(--backend-color-primary, #0bb197) !important;
            background: var(--backend-color-sidebar-hover, #eef3fb) !important;
        }

        .frequent-menus-section,
        .frequent-menu-group,
        .frequent-menu-group-header {
            border-color: var(--backend-color-border-light, #e7ecf3) !important;
        }

        .frequent-menu-group-header {
            background: var(--backend-color-bg-secondary, #f4f7fb) !important;
        }

        .frequent-menu-group-header h6 {
            color: var(--backend-color-text-tertiary, #94a3b8) !important;
        }

        .frequent-menu-item a {
            color: var(--backend-color-sidebar-text, #64748b) !important;
        }

        .frequent-menu-item a:hover {
            color: var(--backend-color-primary, #0bb197) !important;
            background: var(--backend-color-sidebar-hover, #eef3fb) !important;
            border-left-color: var(--backend-color-primary, #0bb197) !important;
        }

        .frequent-menu-item a i {
            color: var(--backend-color-text-tertiary, #94a3b8) !important;
        }

        .frequent-menu-count {
            color: var(--backend-color-text-secondary, #64748b) !important;
            background: color-mix(in srgb, var(--backend-color-bg-secondary, #f4f7fb) 86%, var(--backend-color-border-default, #d8dee8) 14%) !important;
        }

        body.vertical-collpsed .menu-search-submenu,
        body.vertical-collpsed .menu-frequent-submenu,
        body.vertical-collpsed .menu-tabs-content.expanded-right {
            background: var(--backend-color-card-bg, #ffffff) !important;
            border: 1px solid var(--backend-color-border-default, #d8dee8) !important;
            box-shadow: var(--backend-dropdown-shadow, 0 12px 24px rgba(15, 23, 42, 0.15)) !important;
        }
    </style>

    <script>
    (function($) {
        'use strict';

        // ==================== Constants ====================
        var COLLAPSED_CLS = 'vertical-collpsed';
        var HOVER_DELAY = 200;
        var RELATED_AREAS = '#sticky-menu-section, .menu-tabs-content.expanded-right, .menu-tab-btn, .menu-tabs-nav, .menu-search-content, .menu-search-submenu, .menu-search-item, #search-tab-container, #frequent-tab-pane';

        // ==================== Cached DOM ====================
        var $el = {};

        function cacheDOM() {
            $el = {
                body: $('body'),
                sideMenu: $('#side-menu'),
                sidebarMenu: $('#sidebar-menu'),
                stickySection: $('#sticky-menu-section'),
                tabsContent: $('.menu-tabs-content'),
                searchTabContainer: $('#search-tab-container'),
                searchInput: $('#menu-search-input'),
                searchInputMenu: $('#menu-search-input-menu'),
                searchTab: $('#search-menu-tab'),
                frequentTab: $('#frequent-menu-tab'),
                frequentPane: $('#frequent-tab-pane'),
                searchResults: $('#search-results-container'),
                searchResultsList: $('#search-results-list'),
                searchResultsText: $('#search-results-text'),
                searchResultsMenu: $('#search-results-container-menu'),
                searchResultsListMenu: $('#search-results-list-menu'),
                searchResultsTextMenu: $('#search-results-text-menu'),
                frequentSection: $('#frequent-menus-section'),
                simplebar: $('.vertical-menu [data-simplebar]'),
                menuItems: $('#sidebar-menu li'),
                searchMenuItem: $('#menu-search-item'),
                searchSubmenu: $('#menu-search-submenu')
            };
        }

        // ==================== Utility ====================
        function isCollapsed() { return $el.body.hasClass(COLLAPSED_CLS); }
        function isDarkSidebar() { return $el.body.attr('data-sidebar') === 'dark'; }
        function panelBg() { return isDarkSidebar() ? '#1b2c3f' : '#ffffff'; }

        function calculateTop($target) {
            var $s = $target || $el.stickySection;
            if ($s.length) {
                var offset = $s.offset();
                return offset ? (offset.top - $(window).scrollTop()) + 'px' : '0px';
            }
            return '0px';
        }

        function getScrollElement() {
            if ($el.simplebar.length && $el.simplebar[0].simpleBar) {
                return $el.simplebar[0].simpleBar.getScrollElement();
            }
            var $wrapper = $el.simplebar.find('.simplebar-content-wrapper');
            return $wrapper.length ? $wrapper[0] : null;
        }

        function showPanel(top) {
            clearTimer('panelHideTimer');
            state.panelVisible = true;
            if (isCollapsed()) {
                $el.stickySection.css({
                    display: 'block',
                    visibility: 'visible',
                    height: '0',
                    overflow: 'visible',
                    opacity: '1',
                    position: 'static',
                    width: '0'
                });
            }
            $el.tabsContent.stop(true, true).show().addClass('expanded-right').css({
                display: 'block',
                position: 'fixed',
                left: '70px',
                top: top || calculateTop(),
                'min-width': '280px',
                'max-width': '320px',
                'z-index': '1050',
                'background-color': panelBg()
            });
            requestAnimationFrame(function() {
                $el.tabsContent.css({ opacity: '1', visibility: 'visible' });
            });
        }

        function hidePanel(immediate) {
            clearTimer('panelHideTimer');
            state.panelVisible = false;
            if (immediate) {
                if (isCollapsed()) {
                    $el.stickySection.attr('style', '');
                }
                $el.tabsContent.removeClass('expanded-right').hide().css({ opacity: '0', visibility: 'hidden' });
                return;
            }
            $el.tabsContent.stop(true, true).css({ opacity: '0', visibility: 'hidden' });
            state.panelHideTimer = setTimeout(function() {
                if (!state.panelVisible) {
                    if (isCollapsed()) {
                        $el.stickySection.attr('style', '');
                    }
                    $el.tabsContent.removeClass('expanded-right').hide();
                }
            }, 180);
        }

        function activateTab(type) {
            if (type === 'search') {
                $el.frequentPane.hide();
                $el.searchTab.addClass('active');
                $el.frequentTab.removeClass('active');
                if (!$el.searchTabContainer.hasClass('expanded')) {
                    $el.searchTabContainer.addClass('expanded');
                }
            } else {
                $el.frequentPane.show();
                $el.frequentTab.addClass('active');
                $el.searchTab.removeClass('active');
                $el.searchTabContainer.removeClass('expanded');
            }
        }

        function isInArea(target, selectors) {
            return target ? $(target).closest(selectors).length > 0 : false;
        }

        // ==================== State ====================
        var state = {
            isSticky: false, lastScrollTop: 0,
            matchedItems: [], selectedIndex: -1,
            hoverTimer: null, scrollTimer: null, isHovering: false,
            panelVisible: false, panelHideTimer: null,
            searchTimer: null, bodyObserver: null
        };

        function clearTimer(key) {
            if (state[key]) { clearTimeout(state[key]); state[key] = null; }
        }

        function cancelQueuedSearch() {
            clearTimer('searchTimer');
        }

        function focusSearchInput($input, delay) {
            if (!$input || !$input.length) return;
            setTimeout(function() {
                $input.trigger('focus');
                if ($input[0] && typeof $input[0].select === 'function') {
                    $input[0].select();
                }
            }, delay || 0);
        }

        function clearScrollAnimations() {
            var scrollEl = getScrollElement();
            if (scrollEl) $(scrollEl).stop(true, false);
            $('html, body').stop(true, false);
        }

        function collapseMenuItem($li) {
            if (!$li || !$li.length) return;
            var $sub = $li.children('ul.sub-menu');
            if ($sub.length) {
                $sub.removeClass('mm-show mm-collapsing').addClass('mm-collapse')
                    .css({ height: '', display: '', visibility: '' })
                    .attr('aria-expanded', 'false');
                $sub.children('li').each(function() {
                    collapseMenuItem($(this));
                });
            }
            $li.removeClass('mm-active');
            $li.children('a.has-arrow').attr('aria-expanded', 'false');
        }

        function calculatePathMatchScore(currentPath, menuPath) {
            function isSupportedCurrencySegment(segment) {
                var code = String(segment || '').trim().toUpperCase();
                if (!/^[A-Z]{3}$/.test(code)) {
                    return false;
                }
                var cfg = window.__WelineThemeConfig || {};
                var site = window.site || {};
                var welineConfig = (window.Weline && window.Weline.config) || {};
                var supported = {};
                [
                    cfg.availableCurrencies,
                    cfg.supportedCurrencies,
                    cfg.currencyCodes,
                    cfg.currencies,
                    site.availableCurrencies,
                    site.supportedCurrencies,
                    site.currencyCodes,
                    site.currencies,
                    welineConfig.availableCurrencies,
                    welineConfig.supportedCurrencies,
                    welineConfig.currencyCodes,
                    welineConfig.currencies,
                    cfg.defaultCurrency,
                    site.defaultCurrency,
                    site.default_currency
                ].forEach(function(source) {
                    if (Array.isArray(source)) {
                        source.forEach(function(entry) {
                            if (entry && typeof entry === 'object') {
                                entry = entry.code || entry.currency || entry.currency_code || entry.value || '';
                            }
                            var item = String(entry || '').trim().toUpperCase();
                            if (/^[A-Z]{3}$/.test(item)) {
                                supported[item] = true;
                            }
                        });
                        return;
                    }
                    var item = String(source || '').trim().toUpperCase();
                    if (/^[A-Z]{3}$/.test(item)) {
                        supported[item] = true;
                    }
                });
                return supported[code] === true;
            }

            function isLocaleSegment(segment) {
                return /^[a-z]{2}(?:[_-][A-Za-z0-9]{2,8}){1,3}$/i.test(String(segment || ''));
            }

            function getPathname(url) {
                var path;
                try {
                    var a = document.createElement('a');
                    a.href = url;
                    path = a.pathname || '';
                } catch (e) {
                    path = String(url || '');
                }

                path = String(path || '').split(/[?#]/)[0].replace(/\/+$/, '');
                var segments = path.split('/').filter(function(s) { return s.length > 0; });
                if (segments.length > 1 && /^[A-Za-z0-9]{16,}$/.test(segments[0])) {
                    segments = segments.slice(1);
                }
                if (
                    segments.length > 2
                    && isSupportedCurrencySegment(segments[0])
                    && isLocaleSegment(segments[1])
                ) {
                    segments = segments.slice(2);
                }

                return '/' + segments.join('/');
            }

            var currentPathname = getPathname(currentPath);
            var menuPathname = getPathname(menuPath);

            if (currentPathname === menuPathname) return Number.MAX_SAFE_INTEGER;

            var currentSegments = currentPathname.split('/').filter(function(s) { return s.length > 0; });
            var menuSegments = menuPathname.split('/').filter(function(s) { return s.length > 0; });

            if (menuSegments.length > currentSegments.length) return -1;

            var matchCount = 0;
            var minLength = Math.min(currentSegments.length, menuSegments.length);

            for (var i = 0; i < minLength; i++) {
                if (menuSegments[i] !== currentSegments[i]) break;
                matchCount++;
            }

            if (matchCount === menuSegments.length) return matchCount;
            if (menuSegments.length === currentSegments.length && matchCount === menuSegments.length - 1) return matchCount;
            return -1;
        }

        function initActiveMenu() {
            if (!$el.sideMenu.length) return;

            var pageUrl = window.location.href.split(/[?#]/)[0];
            var bestMatch = null;
            var bestScore = -1;
            var $mainMenuLinks = $el.sideMenu.find('a').not(function() {
                return $(this).closest('.menu-search-item, .menu-frequent-item').length > 0;
            });
            var $mainMenu = $el.sideMenu.children('li').not('.menu-search-item, .menu-frequent-item');

            $mainMenu.add($mainMenu.find('li')).removeClass('mm-active');
            $mainMenu.find('a').removeClass('active mm-active');
            $mainMenu.find('ul.sub-menu').removeClass('mm-show').attr('aria-expanded', 'false');

            $mainMenuLinks.each(function() {
                var menuUrl = this.href;
                if (!menuUrl || menuUrl === '#' || menuUrl.indexOf('javascript:') === 0) return;

                var score = calculatePathMatchScore(pageUrl, menuUrl);
                if (score === Number.MAX_SAFE_INTEGER) {
                    bestMatch = $(this);
                    bestScore = score;
                    return false;
                }
                if (score > bestScore) {
                    bestScore = score;
                    bestMatch = $(this);
                }
            });

            if (!bestMatch || bestScore <= 0) return;

            bestMatch.addClass('active');
            var $current = bestMatch.closest('li');
            while ($current.length && $current.closest('#side-menu').length) {
                $current.addClass('mm-active');
                var $sub = $current.children('ul.sub-menu');
                if ($sub.length) {
                    $sub.addClass('mm-show').attr('aria-expanded', 'true');
                }
                var $parentSub = $current.parent('ul.sub-menu');
                if ($parentSub.length) {
                    $parentSub.addClass('mm-show').attr('aria-expanded', 'true');
                }
                $current = $parentSub.parent('li');
            }
        }

        function destroyMetisMenu() {
            if (!$el.sideMenu.length) return;
            var instance = $el.sideMenu.data('metisMenu');
            if (instance && typeof instance.dispose === 'function') {
                instance.dispose();
            }
            $el.sideMenu.off('show.metisMenu');
            $el.sideMenu.off('shown.metisMenu');
            $el.sideMenu.off('hide.metisMenu');
            $el.sideMenu.off('hidden.metisMenu');
            $el.sideMenu.off('.leftSidebarMenu');
            $el.sideMenu.removeData('metisMenu');
        }

        function initSidebarAccordion() {
            if (!$el.sideMenu.length) return;
            $el.sideMenu.off('.leftSidebarMenu');
            $el.sideMenu.on('show.metisMenu.leftSidebarMenu', function(e) {
                var $targetMenu = $(e.target);
                var $topLevelLi = $targetMenu.parent('li').closest('#side-menu > li');
                if (!$topLevelLi.length) return;

                $('#sidebar-menu a.active').removeClass('active');

                $el.sideMenu.children('li').each(function() {
                    var $top = $(this);
                    if ($top[0] === $topLevelLi[0]) return;
                    if ($top.hasClass('menu-search-item') || $top.hasClass('menu-frequent-item') || $top.hasClass('menu-title')) return;
                    if ($top.hasClass('mm-active')) collapseMenuItem($top);
                });
            });
        }

        function initMetisMenu() {
            if (!$el.sideMenu.length) return;

            destroyMetisMenu();
            if (isCollapsed()) {
                $el.sideMenu.find('ul.sub-menu').removeClass('mm-show mm-collapsing')
                    .addClass('mm-collapse')
                    .css({ height: '', display: '', visibility: '' });
                $el.sideMenu.find('li').removeClass('mm-active');
                $el.sideMenu.find('a.has-arrow').attr('aria-expanded', 'false');
                return;
            }

            initActiveMenu();
            $el.sideMenu.metisMenu({ toggle: true });
            initSidebarAccordion();
        }

        function handleCollapseStateChange() {
            cancelQueuedSearch();
            clearTimer('hoverTimer');
            clearTimer('panelHideTimer');
            clearScrollAnimations();

            if (isCollapsed()) {
                $el.searchTabContainer.removeClass('expanded');
                $el.searchInput.val('').blur();
                $el.searchInputMenu.val('');
                resetMenuDisplay();
                $el.searchResults.hide();
                $el.searchResultsMenu.hide();
                hidePanel(true);
            } else {
                initMetisMenu();
                $el.tabsContent.show();
                if ($el.searchTab.hasClass('active')) {
                    expandSearch();
                }
            }
        }

        function initLeftMenuCollapse() {
            $(document).off('click.leftSidebarToggle', '#vertical-menu-btn').on('click.leftSidebarToggle', '#vertical-menu-btn', function() {
                setTimeout(function() {
                    cacheDOM();
                    handleCollapseStateChange();
                }, 140);
            });

            if ($el.body.length && !state.bodyObserver) {
                state.bodyObserver = new MutationObserver(function(mutations) {
                    mutations.forEach(function(mutation) {
                        if (mutation.attributeName === 'class') {
                            cacheDOM();
                            handleCollapseStateChange();
                        }
                    });
                });
                state.bodyObserver.observe($el.body[0], { attributes: true, attributeFilter: ['class'] });
            }
        }

        // ==================== Search Core ====================
        function expandSearch() {
            if (!$el.searchTabContainer.length) return;
            if (isCollapsed()) {
                showPanel();
                activateTab('search');
                $el.searchTabContainer.addClass('expanded');
                focusSearchInput($el.searchInputMenu, 80);
            } else {
                $el.searchTab.addClass('active');
                $el.frequentTab.removeClass('active');
                if (!$el.searchTabContainer.hasClass('expanded')) {
                    $el.searchTabContainer.addClass('expanded');
                    $el.frequentPane.hide();
                    $el.frequentTab.removeClass('active');
                }
                focusSearchInput($el.searchInput, 70);
            }
        }

        function collapseSearch(keepValue) {
            if ($el.searchTabContainer.hasClass('expanded')) {
                $el.searchTabContainer.removeClass('expanded');
                if (!keepValue) {
                    $el.searchInput.val('');
                    $el.searchInputMenu.val('');
                }
                $el.searchInput.blur();
                $el.searchInputMenu.blur();
                $el.searchResults.hide();
                $el.searchResultsMenu.hide();
                resetMenuDisplay();
            }
        }

        function resetMenuDisplay() {
            if (!$el.menuItems.length) $el.menuItems = $('#sidebar-menu li');
            $el.menuItems.removeClass('menu-item-hidden menu-item-matched menu-item-keyboard-selected');
            $el.frequentSection.show();
            $el.searchResults.hide();
        }

        function handleSearch($input) {
            var text = $input.val().toLowerCase().trim();
            state.selectedIndex = -1;
            state.matchedItems = [];

            // 同步两个搜索输入框
            var $other = $input.attr('id') === 'menu-search-input' ? $el.searchInputMenu : $el.searchInput;
            $other.val(text);

            if (!text) {
                $el.searchResults.hide();
                $el.searchResultsMenu.hide();
                resetMenuDisplay();
                return;
            }

            $el.searchResults.show();
            $el.searchResultsMenu.show();
            if (isCollapsed()) { showPanel(); activateTab('search'); }

            performSearch(text);
        }

        function queueSearch($input) {
            cancelQueuedSearch();
            state.searchTimer = setTimeout(function() {
                handleSearch($input);
            }, 70);
        }

        function performSearch(text) {
            var matchCount = 0, matchedItems = [], firstMatch = null;
            var collapsed = isCollapsed();

            $el.frequentSection.hide();
            $el.menuItems.removeClass('menu-item-keyboard-selected');

            $el.menuItems.each(function() {
                var $item = $(this);
                if ($item.hasClass('menu-search-item') || $item.hasClass('menu-frequent-item')) return;

                // 菜单分组标题
                if ($item.hasClass('menu-title')) {
                    var hasMatch = false;
                    $item.nextUntil('.menu-title').each(function() {
                        $(this).find('a span').each(function() {
                            if ($(this).text().toLowerCase().indexOf(text) !== -1) { hasMatch = true; return false; }
                        });
                        if (hasMatch) return false;
                    });
                    if (!collapsed) $item.toggleClass('menu-item-hidden', !hasMatch);
                    return;
                }

                var menuText = $item.find('> a span').text().toLowerCase();
                if (menuText && menuText.indexOf(text) !== -1) {
                    // 直接匹配
                    $item.removeClass('menu-item-hidden').addClass('menu-item-matched');
                    expandParentMenu($item);
                    matchCount++;
                    matchedItems.push({ $item: $item, hasChildren: $item.find('> ul.sub-menu').length > 0 });
                    if (!firstMatch) firstMatch = $item;
                } else {
                    // 搜索子菜单
                    var childMatch = false;
                    $item.find('ul.sub-menu li').each(function() {
                        var $child = $(this);
                        if ($child.hasClass('menu-title')) return;
                        var childText = $child.find('> a span').text().toLowerCase();
                        if (childText && childText.indexOf(text) !== -1) {
                            childMatch = true;
                            matchCount++;
                            $child.removeClass('menu-item-hidden').addClass('menu-item-matched');
                            expandParentMenu($child);
                            matchedItems.push({ $item: $child, hasChildren: $child.find('> ul.sub-menu').length > 0 });
                            if (!firstMatch) firstMatch = $child;
                            // 确保父级可见且展开
                            $item.removeClass('menu-item-hidden');
                            var $sub = $item.find('> ul.sub-menu');
                            if ($sub.length) {
                                $sub.addClass('mm-show').attr('aria-expanded', 'true');
                                $item.addClass('mm-active');
                            }
                        }
                    });
                    if (!collapsed) {
                        if (!childMatch) $item.removeClass('menu-item-matched').addClass('menu-item-hidden');
                        else $item.removeClass('menu-item-hidden');
                    }
                }
            });

            // 展开匹配的父菜单项的子菜单
            var finalItems = matchedItems.slice();
            matchedItems.forEach(function(data) {
                if (data.hasChildren) expandMatchedChildren(data.$item, finalItems);
            });
            matchCount = finalItems.length;

            state.matchedItems = finalItems;
            updateSearchResults(matchCount, finalItems, firstMatch, text);
        }

        function expandMatchedChildren($item, items) {
            var $sub = $item.find('> ul.sub-menu');
            if (!$sub.length) return;
            $sub.addClass('mm-show').attr('aria-expanded', 'true').css({ display: 'block', visibility: 'visible' });
            $item.addClass('mm-active');

            $sub.find('> li').each(function() {
                var $child = $(this);
                if ($child.hasClass('menu-title')) return;
                var isDup = items.some(function(d) { return d.$item[0] === $child[0]; });
                if (!isDup) {
                    $child.removeClass('menu-item-hidden').addClass('menu-item-matched');
                    var hasChildren = $child.find('> ul.sub-menu').length > 0;
                    items.push({ $item: $child, hasChildren: hasChildren });
                    if (hasChildren) expandMatchedChildren($child, items);
                }
            });
        }

        function updateSearchResults(count, items, firstMatch, searchText) {
            var collapsed = isCollapsed();
            if (count > 0) {
                var resultText = '搜索结果 (' + count + ')';
                if (!collapsed) $el.searchResultsText.text(resultText);
                $el.searchResultsTextMenu.text(resultText);

                if (collapsed) {
                    showPanel();
                    activateTab('search');
                    $el.searchResults.show();
                    fillResults($el.searchResultsList, items);
                    fillResults($el.searchResultsListMenu, items);
                } else {
                    fillResults($el.searchResultsList, items);
                }

                // 滚动到第一个匹配项（正常模式下）
                if (firstMatch && !collapsed) {
                    setTimeout(function() {
                        var scrollEl = getScrollElement();
                        if (scrollEl) {
                            var top = firstMatch.offset().top - $(scrollEl).offset().top + scrollEl.scrollTop - 150;
                            $(scrollEl).animate({ scrollTop: top }, 300);
                        }
                    }, 150);
                }
            } else {
                var noResult = '未找到匹配的菜单';
                $el.searchResultsText.text(noResult);
                $el.searchResultsTextMenu.text(noResult);
                $el.searchResultsList.empty();
                $el.searchResultsListMenu.empty();
            }
        }

        function fillResults($list, items) {
            if (!$list.length) return;
            $list.empty();

            // 计算层级并排序（父级在子级前面）
            var sorted = items.map(function(d) {
                var level = 0, $p = d.$item.parent('ul.sub-menu');
                while ($p.length) { level++; $p = $p.parent('li').parent('ul.sub-menu'); }
                return { $item: d.$item, level: level };
            }).sort(function(a, b) {
                // 父子关系：父在前
                if (b.$item.closest(a.$item).length) return -1;
                if (a.$item.closest(b.$item).length) return 1;
                return a.level - b.level;
            });

            sorted.forEach(function(d) {
                var $link = d.$item.find('> a').first();
                if (!$link.length) return;
                var text = $link.find('span').text() || $link.text().trim();
                var icon = $link.find('i').first().attr('class') || 'mdi mdi-circle';
                var href = $link.attr('href') || '#';
                var pad = d.level > 0 ? 'padding-left:' + (20 + d.level * 20) + 'px;' : '';

                $list.append(
                    '<div class="search-result-item px-3 py-2" style="' + pad + '">' +
                    '<a href="' + href + '" class="d-flex align-items-center waves-effect search-result-link">' +
                    '<i class="' + icon + ' me-2"></i><span>' + text + '</span></a></div>'
                );
            });
        }

        function expandParentMenu($item) {
            var $parent = $item.parent('ul.sub-menu');
            if ($parent.length) {
                $parent.addClass('mm-show').attr('aria-expanded', 'true');
                var $parentLi = $parent.parent('li');
                $parentLi.addClass('mm-active');
                expandParentMenu($parentLi);
            }
        }

        function highlightSelected() {
            $el.menuItems.removeClass('menu-item-keyboard-selected');
            if (state.selectedIndex >= 0 && state.selectedIndex < state.matchedItems.length) {
                var $sel = state.matchedItems[state.selectedIndex].$item;
                $sel.addClass('menu-item-keyboard-selected');
                var scrollEl = getScrollElement();
                if (scrollEl && $sel.length) {
                    var rect = $sel[0].getBoundingClientRect();
                    var containerRect = scrollEl.getBoundingClientRect();
                    if (rect.top < containerRect.top || rect.bottom > containerRect.bottom) {
                        var target = scrollEl.scrollTop + (rect.top - containerRect.top) - (scrollEl.clientHeight / 2) + (($sel.outerHeight() || 40) / 2);
                        $(scrollEl).stop(true, false).animate({ scrollTop: Math.max(0, target) }, 140);
                    }
                }
            }
        }

        function handleSearchKeydown(e) {
            var items = state.matchedItems, idx = state.selectedIndex;
            if (e.key === 'Escape') {
                state.selectedIndex = -1;
                state.matchedItems = [];
                if (isCollapsed()) { $(this).val(''); handleSearch($(this)); }
                else collapseSearch();
            } else if (e.key === 'ArrowDown' && items.length) {
                e.preventDefault();
                state.selectedIndex = (idx + 1) % items.length;
                highlightSelected();
            } else if (e.key === 'ArrowUp' && items.length) {
                e.preventDefault();
                state.selectedIndex = idx <= 0 ? items.length - 1 : idx - 1;
                highlightSelected();
            } else if (e.key === 'Enter' && idx >= 0 && idx < items.length) {
                e.preventDefault();
                var $link = items[idx].$item.find('a').first();
                var href = $link.attr('href');
                if (href && href !== 'javascript: void(0);') window.location.href = href;
                else $link.trigger('click');
            }
        }

        // ==================== Init: Search ====================
        function initSearch() {
            $el.searchInput.off('.leftSidebarSearch');
            $el.searchInputMenu.off('.leftSidebarSearch');
            $el.searchTab.off('.leftSidebarSearch');
            $('#search-tab-close-icon').off('.leftSidebarSearch');
            $('#menu-search-link').off('.leftSidebarSearch');
            $('#menu-search-content').off('.leftSidebarSearch');
            $el.searchMenuItem.off('.leftSidebarSearch');
            $el.searchSubmenu.off('.leftSidebarSearch');
            $(document).off('.leftSidebarSearch');

            // Sticky 区域搜索输入框
            $el.searchInput.on('input.leftSidebarSearch', function() { queueSearch($(this)); });
            $el.searchInput.on('keydown.leftSidebarSearch', handleSearchKeydown);

            // 折叠模式下的菜单形式搜索输入框
            $el.searchInputMenu.on('input.leftSidebarSearch', function(e) { e.stopPropagation(); queueSearch($(this)); });
            $el.searchInputMenu.on('keydown.leftSidebarSearch', handleSearchKeydown);
            $el.searchInputMenu.on('click.leftSidebarSearch mousedown.leftSidebarSearch', function(e) { e.stopPropagation(); });

            // 搜索Tab按钮点击
            $el.searchTab.on('click.leftSidebarSearch', function(e) { e.stopPropagation(); expandSearch(); });

            // 关闭图标
            $('#search-tab-close-icon').on('click.leftSidebarSearch', function(e) { e.stopPropagation(); collapseSearch(); });

            // 折叠模式下搜索链接
            $('#menu-search-link').on('click.leftSidebarSearch', function(e) {
                e.preventDefault(); e.stopPropagation(); e.stopImmediatePropagation();
                showPanel();
                activateTab('search');
                focusSearchInput($el.searchInputMenu, 60);
                return false;
            });

            // 阻止搜索内容区域事件冒泡
            $('#menu-search-content').on('click.leftSidebarSearch mousedown.leftSidebarSearch', function(e) { e.stopPropagation(); });

            // 折叠模式下搜索菜单项hover
            $el.searchMenuItem.on('mouseenter.leftSidebarSearch', function() {
                if (isCollapsed()) {
                    $el.searchMenuItem.addClass('mm-active');
                    $el.searchSubmenu.attr('aria-expanded', 'true');
                }
            });

            $el.searchMenuItem.add($el.searchSubmenu).on('mouseleave.leftSidebarSearch', function(e) {
                if (!isCollapsed()) return;
                if (isInArea(e.relatedTarget, '.menu-search-item, .menu-search-submenu, .menu-search-content')) return;
                if ($el.searchInputMenu.is(':focus')) return;
                setTimeout(function() {
                    if (!$el.searchInputMenu.is(':focus')) {
                        $el.searchMenuItem.removeClass('mm-active');
                        $el.searchSubmenu.attr('aria-expanded', 'false');
                    }
                }, 100);
            });

            // 折叠模式下搜索输入框焦点管理
            $el.searchInputMenu.on('focus.leftSidebarSearch', function() {
                if (isCollapsed()) {
                    $el.searchMenuItem.addClass('mm-active');
                    $el.searchSubmenu.attr('aria-expanded', 'true');
                }
            }).on('blur.leftSidebarSearch', function() {
                if (isCollapsed()) {
                    setTimeout(function() {
                        if (!$(document.activeElement).closest('.menu-search-item').length) {
                            $el.searchMenuItem.removeClass('mm-active');
                            $el.searchSubmenu.attr('aria-expanded', 'false');
                        }
                    }, 100);
                }
            });

            // 全局快捷键: Ctrl+K 或 /
            $(document).on('keydown.leftSidebarSearch', function(e) {
                var $t = $(e.target);
                if ($t.is('input, textarea, [contenteditable="true"]')) return;
                if ((e.ctrlKey && e.key === 'k') || e.key === '/') {
                    e.preventDefault();
                    if (isCollapsed()) {
                        showPanel();
                        activateTab('search');
                        focusSearchInput($el.searchInputMenu, 60);
                    } else {
                        expandSearch();
                    }
                }
            });

            // 点击外部收起搜索
            $(document).on('click.leftSidebarSearch', function(e) {
                if (!$el.searchTabContainer.is(e.target) && !$el.searchTabContainer.has(e.target).length) {
                    if ($el.searchInput.val().trim() === '' && $el.searchTabContainer.hasClass('expanded')) {
                        collapseSearch();
                    }
                }
            });
        }

        // ==================== Init: Tabs ====================
        function initTabs() {
            var searchHoverTimer = null;

            $el.searchTab.off('.leftSidebarTabs');
            $el.searchTabContainer.off('.leftSidebarTabs');
            $el.frequentTab.off('.leftSidebarTabs');
            $(document).off('.leftSidebarTabs');

            // 默认展开搜索（正常模式下）
            setTimeout(function() { if (!isCollapsed()) expandSearch(); }, 300);

            // 搜索Tab hover
            $el.searchTab.on('mouseenter.leftSidebarTabs', function(e) {
                if (isCollapsed()) {
                    expandSearch();
                } else {
                    var rt = e.relatedTarget;
                    if (rt && !$(rt).closest('#sticky-menu-section').length && $el.frequentTab.hasClass('active')) return;
                    $el.searchTab.addClass('active');
                    $el.frequentTab.removeClass('active');
                }
            });

            // 搜索Tab/容器鼠标离开（正常模式下收起空搜索框）
            function handleSearchLeave(e) {
                if (isCollapsed()) return;
                if (isInArea(e.relatedTarget, '#search-tab-container, .menu-tabs-content, #search-results-container')) return;
                clearTimeout(searchHoverTimer);
                searchHoverTimer = setTimeout(function() {
                    if ($el.searchInput.val().trim() === '' && $el.searchTabContainer.hasClass('expanded')) {
                        $el.searchTabContainer.removeClass('expanded');
                        $el.searchInput.blur();
                    }
                }, HOVER_DELAY);
            }
            $el.searchTab.on('mouseleave.leftSidebarTabs', handleSearchLeave);
            $el.searchTabContainer.on('mouseleave.leftSidebarTabs', handleSearchLeave)
                .on('mouseenter.leftSidebarTabs', function() { clearTimeout(searchHoverTimer); });

            // 保持内容展开
            $(document).on('mouseenter.leftSidebarTabs', '#menu-search-input, #search-tab-container, #frequent-tab-pane', function() {
                if (!isCollapsed()) clearTimeout(searchHoverTimer);
            });

            // 常用菜单Tab hover
            $el.frequentTab.on('mouseenter.leftSidebarTabs', function() {
                if (isCollapsed()) { showPanel(); activateTab('frequent'); }
                else { $el.frequentTab.addClass('active'); $el.searchTab.removeClass('active'); }
            });

            // 常用菜单Tab点击
            $el.frequentTab.on('click.leftSidebarTabs', function() {
                activateTab('frequent');
                collapseSearch(true);
                $el.searchResults.hide();
                if (isCollapsed()) showPanel();
            });
        }

        // ==================== Init: Sticky ====================
        function initSticky() {
            $el.stickySection.off('.leftSidebarSticky');
            $('.menu-tab-btn').off('.leftSidebarSticky');
            $(document).off('.leftSidebarSticky');

            // Sticky区域hover
            $el.stickySection.on('mouseenter.leftSidebarSticky', function() {
                state.isHovering = true;
                clearTimer('hoverTimer');
                if (isCollapsed()) {
                    showPanel();
                    activateTab('search');
                } else {
                    if ($el.searchTab.hasClass('active')) {
                        if (!$el.searchTabContainer.hasClass('expanded')) {
                            $el.searchTabContainer.addClass('expanded');
                            focusSearchInput($el.searchInput, 60);
                        }
                    } else if ($el.frequentTab.hasClass('active')) {
                        $el.frequentPane.show();
                        $el.searchTabContainer.removeClass('expanded');
                    }
                }
            });

            // 展开面板hover
            $(document).on('mouseenter.leftSidebarSticky', '.menu-tabs-content.expanded-right', function() {
                state.isHovering = true;
                clearTimer('hoverTimer');
            });

            // 统一的鼠标离开处理
            function handleLeave(e) {
                var rt = e.relatedTarget;
                if (isInArea(rt, RELATED_AREAS)) return;
                state.hoverTimer = setTimeout(function() {
                    var ct = document.elementFromPoint(e.clientX || 0, e.clientY || 0);
                    if (isInArea(ct, RELATED_AREAS)) return;
                    state.isHovering = false;
                    collapseContent();
                }, isCollapsed() ? 100 : HOVER_DELAY);
            }

            $el.stickySection.on('mouseleave.leftSidebarSticky', handleLeave);
            $(document).on('mouseleave.leftSidebarSticky', '.menu-tabs-content.expanded-right', handleLeave);
            $(document).on('mouseleave.leftSidebarSticky', '.menu-search-item, .menu-search-submenu, .menu-search-content', function(e) {
                if (isCollapsed()) handleLeave(e);
            });

            function collapseContent() {
                if (isCollapsed()) {
                    hidePanel();
                } else {
                    if ($el.searchTabContainer.hasClass('expanded') && $el.searchInput.val().trim() === '') {
                        $el.searchTabContainer.removeClass('expanded');
                        $el.searchInput.blur();
                    }
                    $el.frequentPane.hide();
                }
                if ($el.searchInput.val().trim() === '') $el.searchResults.hide();
            }

            // Tab按钮点击
            $('.menu-tab-btn').on('click.leftSidebarSticky', function() {
                state.isHovering = true;
                if (isCollapsed()) showPanel();
                else $el.tabsContent.removeClass('collapsed');
                clearTimer('hoverTimer');
                clearTimer('scrollTimer');
            });

            // 滚动监听
            var scrollEl = getScrollElement();
            if (scrollEl) $(scrollEl).off('scroll.leftSidebarSticky').on('scroll.leftSidebarSticky', checkSticky);
            else $(window).off('scroll.leftSidebarSticky').on('scroll.leftSidebarSticky', checkSticky);
            setTimeout(checkSticky, 100);
        }

        function checkSticky() {
            var scrollEl = getScrollElement();
            var scrollTop = scrollEl ? scrollEl.scrollTop : $(window).scrollTop();

            if (scrollTop > 10) {
                if (!state.isSticky) { state.isSticky = true; $el.stickySection.addClass('sticky-active'); }
                if (scrollTop > state.lastScrollTop + 2) $el.tabsContent.addClass('collapsed');
                else if (scrollTop < state.lastScrollTop - 2 && scrollTop < 50) $el.tabsContent.removeClass('collapsed');
            } else if (state.isSticky) {
                state.isSticky = false;
                $el.stickySection.removeClass('sticky-active');
                $el.tabsContent.removeClass('collapsed');
            }

            state.lastScrollTop = scrollTop;
            clearTimer('scrollTimer');
            state.scrollTimer = setTimeout(function() {
                var cur = scrollEl ? scrollEl.scrollTop : $(window).scrollTop();
                if (state.isSticky && cur < 100) $el.tabsContent.removeClass('collapsed');
            }, 500);
        }

        // ==================== Init: Sidebar Collapse ====================
        function initSidebarCollapse() {
            initLeftMenuCollapse();
            handleCollapseStateChange();
        }

        // ==================== Active Menu: Expansion & Scroll ====================
        // 菜单定位：负责 #side-menu 内部激活项的展开和滚动居中
        window.scrollToActiveMenu = function(retryCount) {
            retryCount = retryCount || 0;
            if (!$el.sideMenu || !$el.sideMenu.length) return;

            // 找到最深层的激活叶子链接（a.active 且不带 has-arrow 标识为叶子节点）
            var $activeLink = $el.sideMenu.find('a.active').not('.has-arrow').first();
            var $activeMenu;

            if ($activeLink.length) {
                $activeMenu = $activeLink.closest('li');
            } else {
                // 后备方案：取最深层的 mm-active li（DOM 中最后一个 = 最深层级）
                $activeMenu = $el.sideMenu.find('li.mm-active').last();
            }

            if (!$activeMenu || !$activeMenu.length) return;

            var scrollElement = getScrollElement();
            if (!scrollElement) {
                if (retryCount < 1) setTimeout(function() { window.scrollToActiveMenu(1); }, 400);
                return;
            }

            setTimeout(function() {
                try {
                    // 确保所有父级菜单都已展开（修复三级菜单展开问题）
                    $activeMenu.parents('li').each(function() {
                        var $li = $(this);
                        if (!$li.closest('#side-menu').length) return false;
                        $li.addClass('mm-active');
                        var $sub = $li.find('> ul.sub-menu');
                        if ($sub.length) $sub.addClass('mm-show').attr('aria-expanded', 'true');
                        $li.find('> a.has-arrow').addClass('mm-active');
                    });
                    $activeMenu.addClass('mm-active');

                    // 滚动到激活项居中
                    setTimeout(function() {
                        try {
                            var el = $activeMenu[0];
                            if (!el) return;
                            var mRect = el.getBoundingClientRect();
                            var cRect = scrollElement.getBoundingClientRect();
                            var mH = $activeMenu.outerHeight() || 40;
                            var target = scrollElement.scrollTop + (mRect.top - cRect.top) - (scrollElement.clientHeight / 2) + (mH / 2);
                            target = Math.max(0, Math.min(target, scrollElement.scrollHeight - scrollElement.clientHeight));
                            $(scrollElement).animate({ scrollTop: target }, 400);
                        } catch (e) { console.warn('菜单定位滚动出错:', e); }
                    }, 300);
                } catch (e) { console.warn('菜单定位展开出错:', e); }
            }, 200);
        };

        // ==================== Bootstrap ====================
        $(document).ready(function() {
            cacheDOM();
            initMetisMenu();
            initTabs();
            initSearch();
            initSticky();
            initSidebarCollapse();
            scrollToActiveMenu();

            // 监听 mm-active 变化，自动滚动到新激活的菜单
            if (typeof MutationObserver !== 'undefined' && $el.sideMenu[0]) {
                var scrollDebounce = null, lastActiveId = null;
                new MutationObserver(function(muts) {
                    var shouldScroll = false, activeId = null;
                    muts.forEach(function(m) {
                        if (m.attributeName !== 'class') return;
                        var old = m.oldValue || '', cur = m.target.className || '';
                        if (old.indexOf('mm-active') === -1 && cur.indexOf('mm-active') !== -1 && $(m.target).is('li')) {
                            var $li = $(m.target);
                            if ($li.closest('#side-menu').length) {
                                var $a = $li.find('> a.active, .active').first();
                                activeId = ($a.length ? $a.attr('href') : $li.find('> a').first().attr('href')) || $li.text();
                                if (activeId && activeId !== lastActiveId) shouldScroll = true;
                            }
                        }
                    });
                    if (shouldScroll) {
                        lastActiveId = activeId;
                        clearTimeout(scrollDebounce);
                        scrollDebounce = setTimeout(function() {
                            if (typeof window.scrollToActiveMenu === 'function') window.scrollToActiveMenu();
                        }, 300);
                    }
                }).observe($el.sideMenu[0], {
                    attributes: true, attributeFilter: ['class'], subtree: true, attributeOldValue: true
                });
            }
        });

        // DOMContentLoaded 后备
        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', function() {
                setTimeout(function() { if (typeof window.scrollToActiveMenu === 'function') window.scrollToActiveMenu(); }, 300);
            });
        } else {
            setTimeout(function() { if (typeof window.scrollToActiveMenu === 'function') window.scrollToActiveMenu(); }, 300);
        }

    })(jQuery);
    </script>
<?php
endif; ?>
